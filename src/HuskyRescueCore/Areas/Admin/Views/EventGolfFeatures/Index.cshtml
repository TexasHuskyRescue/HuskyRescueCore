@model IEnumerable<HuskyRescueCore.Models.EventGolfFeatures>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-area="Admin" asp-controller="EventGolfFeatures" asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Feature)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Feature)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { AreaAttribute="Admin" /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Details", "Details", new { AreaAttribute = "Admin" /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete", new { AreaAttribute = "Admin" /* id=item.PrimaryKey */ })
            </td>
        </tr>
}
    </tbody>
</table>
